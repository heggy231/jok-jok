import { Route, Switch } from "react-router-dom";
import Routes from '../../routes';

const Main = () => {
  return (
    <main>
      <Switch>
        
      </Switch>
    </main>
  );
};

export default Main;
